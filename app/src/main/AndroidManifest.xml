<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.osgeo">

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Actvities.BrowseActivity"></activity>
        <activity android:name=".Actvities.DetailsActivity" />
        <activity android:name=".Actvities.GitHubLoginActivity" />
        <activity android:name=".Actvities.TracLoginActivity" />
        <activity android:name=".Actvities.HomeActivity" />
        <activity
            android:name=".Actvities.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>